
#ifndef __P3_HEADER_H__
#define __P3_HEADER_H__

int checkNumber(int _num, int _digits);			// check _num if it consists of two factor which of each is _digits digits.

int getPairFactor(int _orig, int _factor1);		// check _factor1 if it is the factor of _origNum and return it's pair factor
												// if _factor1 is not the factor of _origNum, then return 0;
bool checkDigits(int _tarNum, int _digits);		// check if the target number is _digits number.












#endif